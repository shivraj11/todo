import { FormValidDirective } from './form-valid.directive';

describe('FormValidDirective', () => {
  it('should create an instance', () => {
    const directive = new FormValidDirective();
    expect(directive).toBeTruthy();
  });
});
